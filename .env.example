# Copy this file to .env and fill in your values
# .env is gitignored — never commit real credentials

APP_PORT=8084
LOG_LEVEL=info

DB_HOST=postgres
DB_PORT=5432
DB_USER=ledger
DB_PASSWORD=ledger
DB_NAME=ledger
DB_SSLMODE=disable
